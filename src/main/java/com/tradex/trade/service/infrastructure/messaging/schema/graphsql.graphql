type AllocationLeg {
  organizationId: ID!
  allocatedQuantity: BigDecimal!
  allocatedNotional: BigDecimal!
  currency: String!
}

type Allocation {
  tradeExecutionId: ID!
  status: String!
  ruleCode: String
  ruleVersion: String
  legs: [AllocationLeg!]!
}

type Query {
  allocationByTrade(tradeExecutionId: ID!): Allocation
}
